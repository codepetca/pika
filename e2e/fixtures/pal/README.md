# Pal visual fixtures

Two unchanged badge assets copied from the read-only Pal checkout at `69c3c91`
(`apps/web/public/assets/badges/`). They match `@codepet/pal-widget` alpha.6's
existing fixture snapshot. The browser isolation test serves them locally,
without contacting or provisioning the provider. Product assets still come from
the configured Pal service; these files are test-only.
