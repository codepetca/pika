# CI rollout proof

This temporary pull request verifies that documentation-only changes satisfy
both transition-required contexts: `Test & Build` and the aggregate `PR Gate`.
It is a rollout probe and will be closed without merging after the checks pass.
